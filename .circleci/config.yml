version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6.2-stretch-browsers
    steps:
      - checkout
      - run:
          name: Download Selenium
          command: |
            curl -O https://stormrunner-load.saas.hpe.com/v2/load-tests?TENANTID=949194025&projectId=109

